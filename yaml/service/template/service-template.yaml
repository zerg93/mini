- route-template:
    id: service-template
    parameters:
      - name: RestPath
    from:
      uri: rest:post:{{RestPath}}
      steps:
        - log: ${headers}
        - convertBodyTo:
            type: String
        - log: ${body}